apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "tls-service.fullname" . }}-test-connection"
  labels:
    {{- include "tls-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "tls-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
